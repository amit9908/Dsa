unordered_map<char, int> map; // creating map


// traversing over key
for (const auto& entry : map) {
    str += entry.first;
}

for(auto it=cnt.begin() ; it != cnt.end(); i++){
    if(it.second >= quantity[index])
}




// mapping character with value
string st = words[i];
for (int j = 0; j < st.length(); j++)
{
    char ch = st[j];
    map[ch] = nullptr;
}


string st = words[i];
for (int j = 0; j < st.length(); j++)
{
    char ch = st[j];
    map[ch] = nullptr;
}
